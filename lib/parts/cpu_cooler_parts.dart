import 'package:flutter/material.dart';

class PartsCPUCooler extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CPUクーラー'),
      ),
      body: const Center(child: Text('CPUクーラー')),
    );
  }
}