import 'package:flutter/material.dart';

class PartsPowerSupply extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('電源ユニット'),
      ),
      body: const Center(child: Text('電源ユニット')),
    );
  }
}