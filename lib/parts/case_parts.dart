import 'package:flutter/material.dart';

class PartsCase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ケース'),
      ),
      body: const Center(child: Text('ケース')),
    );
  }
}