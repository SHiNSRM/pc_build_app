import 'package:flutter/material.dart';

class PartsStorage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ストレージ'),
      ),
      body: const Center(child: Text('ストレージ')),
    );
  }
}