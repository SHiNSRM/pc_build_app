import 'package:flutter/material.dart';

class PartsCaseFan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ケースファン'),
      ),
      body: const Center(child: Text('ケースファン')),
    );
  }
}