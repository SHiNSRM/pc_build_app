import 'package:flutter/material.dart';

class PartsCPU extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CPU'),
      ),
      body: const Center(child: Text('CPU')),
    );
  }
}