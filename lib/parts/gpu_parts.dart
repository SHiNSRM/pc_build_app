import 'package:flutter/material.dart';

class PartsGPU extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GPU'),
      ),
      body: const Center(child: Text('GPU')),
    );
  }
}