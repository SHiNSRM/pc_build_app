import 'package:flutter/material.dart';

class Build extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('組み立て'),
      ),
      body: const Center(child: Text('組み立て')),
    );
  }
}
